import React, {FC} from 'react';
import styles from './AsidePanel.module.scss'

const AsidePanel: FC = (props) => {
    return (
        <aside style={styles}>

        </aside>
    );
};

export default AsidePanel;